engine: QLever
map_view_url: https://qlever.dev/petrimaps/
query_templates:
  subject_completion: |
    #+ title: Qlue-ls subject completion query.
    #+ description: This query was generated by the SPARQL language server Qlue-ls.
    {% include "prefix_declarations" %}

    PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?qlue_ls_entity
           (SAMPLE(?name) AS ?qlue_ls_label)
           (GROUP_CONCAT(DISTINCT ?alias; SEPARATOR=", ") AS ?qlue_ls_alias)
           (COUNT(*) AS ?qlue_ls_count) WHERE {
      {
        SELECT ?qlue_ls_entity WHERE {
          ?qlue_ls_entity rdfs:label ?name
          FILTER (LANG(?name) = "en")
          FILTER (REGEX(STR(?name),"^{{ search_term }}"))
        }
      }
      UNION {
        SELECT ?qlue_ls_entity WHERE {
          ?qlue_ls_entity skos:altLabel ?name
          FILTER (LANG(?name) = "en")
          FILTER (REGEX(STR(?name),"^{{ search_term }}"))
        }
      }
      ?qlue_ls_entity rdfs:label ?name ;
                      ?p ?o ;
      OPTIONAL {
        ?qlue_ls_entity skos:altLabel ?alias
        FILTER (LANG(?alias) = "en")
      }
      FILTER (LANG(?name) = "en")
    }
    GROUP BY ?qlue_ls_entity
    ORDER BY DESC(?qlue_ls_count)
    LIMIT {{ limit }} OFFSET {{ offset }}
  predicate_completion_context_sensitive: |-
    # predicate completion sensitive
    {% for prefix in prefixes %}
    PREFIX {{prefix.0}}: <{{prefix.1}}>
    {% endfor %}
    {% if subject is not variable %}
      PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
      PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
      SELECT ?qlue_ls_entity
             (SAMPLE(?name) as ?qlue_ls_label)
             (SAMPLE(?count) as ?qlue_ls_count) WHERE {
        {
          SELECT ?qlue_ls_entity (COUNT(?qlue_ls_entity) AS ?count) WHERE {
            {{ local_context }}
          }
          GROUP BY ?qlue_ls_entity
        }
        {% if not search_term %}
          ?qui_tmp_1 ?qui_tmp_2 ?qlue_ls_entity .
          ?qui_tmp_1 rdfs:label ?name . FILTER (LANG(?name) = "en")
        BIND (?name AS ?alias)
        {% elif search_term_uncompressed %}
          ?qui_tmp_1 ?qui_tmp_2 ?qlue_ls_entity .
          ?qui_tmp_1 rdfs:label ?name FILTER (LANG(?name) = "en")
        FILTER REGEX(STR(?qlue_ls_entity), "{{ search_term_uncompressed }}", "i")
        {% else %}
          ?qui_tmp_1 ?qui_tmp_2 ?qlue_ls_entity .
          ?qui_tmp_1 rdfs:label ?name FILTER (LANG(?name) = "en")
          OPTIONAL {
            ?qui_tmp_1 skos:altLabel ?alias FILTER (LANG(?alias) = "en")
          }
          FILTER (REGEX(STR(?name), "{{ search_term }}", "i") || REGEX(STR(?alias), "{{ search_term }}", "i"))
        {% endif %}
      }
      GROUP BY ?qlue_ls_entity
      ORDER BY DESC(?qlue_ls_count)
    {% else %}
      PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
      PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
      SELECT ?qlue_ls_entity
             (SAMPLE(?name) as ?qlue_ls_label)
             (SAMPLE(?count) as ?qlue_ls_count) WHERE {
        {% if not context %}
          {
            SELECT ?qlue_ls_entity (COUNT(?qlue_ls_entity) AS ?count) WHERE {
              {{ local_context }}
            }
            GROUP BY ?qlue_ls_entity
          }
        {% else %}
          {
            SELECT ?qlue_ls_entity (COUNT(DISTINCT {{ subject }}) AS ?count) WHERE {
             {{ context }} {{ local_context }}
            }
            GROUP BY ?qlue_ls_entity
          }
        {% endif %}
        {% if not search_term %}
          ?qui_tmp_1 ?qui_tmp_2 ?qlue_ls_entity .
          ?qui_tmp_1 rdfs:label ?name FILTER (LANG(?name) = "en")
          BIND (?name AS ?alias)
        {% elif search_term_uncompressed %}
          ?qui_tmp_1 ?qui_tmp_2 ?qlue_ls_entity .
          ?qui_tmp_1 rdfs:label ?name FILTER (LANG(?name) = "en")
          FILTER REGEX(STR(?qlue_ls_entity), "{{ search_term_uncompressed }}", "i")
        {% else %}
        ?qui_tmp_1 ?qui_tmp_2 ?qlue_ls_entity .
        ?qui_tmp_1 rdfs:label ?name FILTER (LANG(?name) = "en")
        OPTIONAL {
          ?qui_tmp_1 skos:altLabel ?alias FILTER (LANG(?alias) = "en")
        }
        FILTER (REGEX(STR(?name), "{{ search_term }}", "i") || REGEX(STR(?alias), "{{ search_term }}", "i"))
        {% endif %}
      }
      GROUP BY ?qlue_ls_entity
      ORDER BY DESC(?qlue_ls_count)
    {% endif %}
    LIMIT {{ limit }} OFFSET {{ offset }}
  predicate_completion_context_insensitive: |-
    {% include "prefix_declarations" %}
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?qlue_ls_entity ?qlue_ls_label ?qlue_ls_alias ?qlue_ls_count WHERE {
      {
        SELECT ?qlue_ls_entity (COUNT(*) AS ?qlue_ls_count) WHERE {
          {{ local_context }}
        }
        GROUP BY ?qlue_ls_entity
        ORDER BY DESC(?qlue_ls_count)
        LIMIT {{ limit }}
      }
      OPTIONAL { ?qlue_ls_entity rdfs:label ?qlue_ls_label }
      OPTIONAL { ?qlue_ls_entity rdfs:comment ?qlue_ls_alias }
    }
  object_completion_context_sensitive: |-
    #+ title: Qlue-ls object sensitive completion query.
    #+ description: This query was generated by the SPARQL language server Qlue-ls.
    {% include "prefix_declarations" %}
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?qlue_ls_entity ?qlue_ls_label ?qlue_ls_alias ?qlue_ls_count WHERE {
      {
        SELECT ?qlue_ls_entity (COUNT(*) AS ?qlue_ls_count) WHERE {
          {{ context }} {{ local_context }}
        }
        GROUP BY ?qlue_ls_entity
        ORDER BY DESC(?qlue_ls_count)
        LIMIT {{ limit }}
      }
      OPTIONAL { ?qlue_ls_entity rdfs:label ?qlue_ls_label }
      OPTIONAL { ?qlue_ls_entity rdfs:comment ?qlue_ls_alias }
      {% if search_term %}
      FILTER(CONTAINS(LCASE(STR(?qlue_ls_label)), LCASE("{{ search_term }}")) || CONTAINS(LCASE(STR(?qlue_ls_alias)), LCASE("{{ search_term }}")))
      {% endif %}
    }
  object_completion_context_insensitive: |-
    {% include "prefix_declarations" %}
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?qlue_ls_entity ?qlue_ls_label ?qlue_ls_alias ?qlue_ls_count WHERE {
      {
        SELECT ?qlue_ls_entity (COUNT(*) AS ?qlue_ls_count) WHERE {
          {{ local_context }}
        }
        GROUP BY ?qlue_ls_entity
        ORDER BY DESC(?qlue_ls_count)
        LIMIT {{ limit }}
      }
      OPTIONAL { ?qlue_ls_entity rdfs:label ?qlue_ls_label }
      OPTIONAL { ?qlue_ls_entity rdfs:comment ?qlue_ls_alias }
      {% if search_term %}
      FILTER(CONTAINS(LCASE(STR(?qlue_ls_label)), LCASE("{{ search_term }}")) || CONTAINS(LCASE(STR(?qlue_ls_alias)), LCASE("{{ search_term }}")))
      {% endif %}
    }
  values_completion_context_sensitive: |-
    {% include "prefix_declarations" %}
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?qlue_ls_entity ?qlue_ls_label ?qlue_ls_alias WHERE {
      {{ context }} {{ local_context }}
      OPTIONAL { ?qlue_ls_entity rdfs:label ?qlue_ls_label }
      OPTIONAL { ?qlue_ls_entity rdfs:comment ?qlue_ls_alias }
      {% if search_term %}
      FILTER(CONTAINS(LCASE(STR(?qlue_ls_label)), LCASE("{{ search_term }}")) || CONTAINS(LCASE(STR(?qlue_ls_entity)), LCASE("{{ search_term }}")))
      {% endif %}
    }
    LIMIT {{ limit }}
  values_completion_context_insensitive: |-
    {% include "prefix_declarations" %}
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?qlue_ls_entity ?qlue_ls_label ?qlue_ls_alias WHERE {
      {{ local_context }}
      OPTIONAL { ?qlue_ls_entity rdfs:label ?qlue_ls_label }
      OPTIONAL { ?qlue_ls_entity rdfs:comment ?qlue_ls_alias }
      {% if search_term %}
      FILTER(CONTAINS(LCASE(STR(?qlue_ls_label)), LCASE("{{ search_term }}")) || CONTAINS(LCASE(STR(?qlue_ls_entity)), LCASE("{{ search_term }}")))
      {% endif %}
    }
    LIMIT {{ limit }}
  hover: |-
    {% include "prefix_declarations" %}
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?qlue_ls_label ?qlue_ls_alias WHERE {
      {{ entity }} rdfs:label ?qlue_ls_label .
      OPTIONAL { {{ entity }} rdfs:comment ?qlue_ls_alias }
    }
    LIMIT 1
